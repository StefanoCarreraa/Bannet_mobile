import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bannet_mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
